<%= form_with model: @issuer, local: true do |form| %>

  <% if @issuer.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@issuer.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @issuer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>


  <p>
    <%= form.submit %>
  </p>

<% end %>