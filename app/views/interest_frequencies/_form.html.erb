<%= form_with model: @frequency, local: true do |form| %>

  <% if @frequency.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@frequency.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @frequency.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :frequency %><br>
    <%= form.text_field :frequency %>
  </p>


  <p>
    <%= form.submit %>
  </p>

<% end %>